TickTasks is a simple Android task list app. It uses Android native components and an embedded SQLite database (via Room) to store tasks. The UI uses Material3 styles and a RecyclerView for the task list.

Room (SQLite abstraction for persistence)
RecyclerView (list UI)
